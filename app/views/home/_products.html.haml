:scss
  .sidebar{
    background-color: whitesmoke;
    height: 350px;
    //position: fixed;
    margin: 0;
    ul{
      padding: 0;
      a{
        text-decoration: none;
        li{
          margin: 5px;
          background-color: #999999;
          padding: 10px 20px;
          display: block;
        }

      }

    }
  }

.sidebar.col-xs-3
  %ul
    -@products_group_by_categories.each do |category, products|
      = link_to products_by_category_path(category), remote: true do
        %li= category.title


.products-content.col-xs-9
  -#- binding.pry
  =render partial: 'products/by_categories', locals: {products: @products_group_by_categories.first.second}
  -#-@products_group_by_categories.first.second.each do |product|
  -#  = link_to '#' do
  -#    = render partial: 'product_card', locals: {product: product}

